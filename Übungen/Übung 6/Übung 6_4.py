# Satz Analyse
satz = []
def satz_analyse(text):
    satz = text.split(" ")
    print(f"Der Satz hat {len(satz)} Wörter")
    for i in satz:
        



satz_analyse(str(input("Geben Sie einen Satz ein: ")))